server:
  port: 10888
eureka:
  client:
    service-url:
      default-zone: 'peer1:8761/eureka/,peer2:8762/eureka/'
  instance:
    metadata-map:
      cluster: prontera
    prefer-ip-address: false
spring:
  cloud:
    config:
      server:
        git:
          uri: 'http://prontera:10080/chris/config-repo.git'
          #searchPaths: '{application}'
          clone-on-start: true
          username: 'chris'
          password: '123123'
        encrypt:
          enabled: false
  application:
    name: 'config-server'
encrypt:
  key: '*&3loa#@9no_0*(!'
